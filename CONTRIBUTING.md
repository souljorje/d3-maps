# Contributing

1. Install deps: `pnpm install`.
2. Start docs dev server to see changes: `pnpm docs:dev`.
3. Make your changes.
4. Run checks: `pnpm lint && pnpm test && pnpm build`.
5. Add a changeset: `pnpm changeset`, select package + bump type, write summary.
6. Open a PR to `main`.
