{
  "name": "d3-maps",
  "type": "module",
  "private": true,
  "packageManager": "pnpm@10.25.0",
  "engines": {
    "node": ">= 22"
  },
  "scripts": {
    "dev": "pnpm --filter docs dev",
    "build": "turbo run build",
    "build:packages": "turbo run build --filter=!docs",
    "test": "turbo run test",
    "docs:dev": "pnpm --filter docs dev",
    "docs:gen:examples": "node scripts/docs-gen-examples.mjs",
    "typedoc": "typedoc --options typedoc.json",
    "docs:gen": "pnpm docs:gen:examples && pnpm typedoc",
    "docs:build": "pnpm docs:gen && pnpm --filter docs build",
    "docs:preview": "pnpm --filter docs preview",
    "changeset": "changeset",
    "version": "changeset version",
    "release": "changeset publish",
    "lint": "eslint ./packages --max-warnings 0 --cache",
    "lintfix": "pnpm run lint --fix"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@antfu/eslint-config": "catalog:",
    "@changesets/changelog-github": "catalog:",
    "@changesets/cli": "catalog:",
    "@vue/tsconfig": "catalog:",
    "eslint": "catalog:",
    "lint-staged": "catalog:",
    "remark-insert-headings": "catalog:",
    "remark-toc": "catalog:",
    "tsdown": "catalog:",
    "turbo": "catalog:",
    "typedoc": "catalog:",
    "typedoc-plugin-markdown": "catalog:",
    "typedoc-plugin-remark": "catalog:"
  },
  "gitHooks": {
    "pre-commit": "lint-staged",
    "pre-push": "pnpm docs:build"
  },
  "lint-staged": {
    "*.{ts,tsx,vue}": [
      "eslint --fix --max-warnings=0"
    ]
  }
}
