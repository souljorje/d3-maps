{
  "name": "d3-maps",
  "type": "module",
  "private": true,
  "packageManager": "pnpm@11.0.0",
  "engines": {
    "node": ">= 22"
  },
  "scripts": {
    "dev": "pnpm --filter docs dev",
    "build": "turbo run build --filter=!@d3-maps/docs",
    "build:full": "turbo run build",
    "build:docs": "turbo run build --filter=@d3-maps/docs",
    "docs:preview": "pnpm --filter docs preview",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck",
    "typecheck:test": "turbo run typecheck:test",
    "changeset": "changeset",
    "version": "changeset version",
    "release": "changeset publish",
    "prepare": "simple-git-hooks",
    "lint-staged": "lint-staged",
    "lint": "eslint ./packages --max-warnings 0 --cache",
    "lintfix": "pnpm run lint --fix"
  },
  "devDependencies": {
    "@antfu/eslint-config": "catalog:",
    "@changesets/changelog-github": "catalog:",
    "@changesets/cli": "catalog:",
    "@tsdown/css": "catalog:",
    "@vue/tsconfig": "catalog:",
    "eslint": "catalog:",
    "lint-staged": "catalog:",
    "simple-git-hooks": "catalog:",
    "tsdown": "catalog:",
    "turbo": "catalog:"
  },
  "simple-git-hooks": {
    "pre-commit": "pnpm exec lint-staged && pnpm test"
  },
  "lint-staged": {
    "*.{ts,tsx,vue}": [
      "eslint --fix --max-warnings=0"
    ]
  }
}
